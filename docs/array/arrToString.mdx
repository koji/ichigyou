---
title: arrToString
description: Converts an array to a string.
---

## Signature

```typescript path=null start=null
arrToString(arr: any[]): string
```

## Parameters

- **arr**: `any[]` - The array to be converted.

## Returns

`string` - The input array converted to a string, with elements separated by commas.

## Example

```typescript path=null start=null
const result = arrToString(['hello', 'world'])
console.log(result) // 'hello,world'
```

## Source

Defined in [array.ts:36](https://github.com/koji/ichigyou/blob/main/src/array.ts#L36)
